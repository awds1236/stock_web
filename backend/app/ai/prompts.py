"""AI 분석 프롬프트.

이 파일의 설계 목표는 하나입니다: **모델이 숫자를 지어내지 못하게 하는 것.**

네 가지 장치를 씁니다.

  1. 모델에게 검색이나 기억을 요구하지 않습니다. 실적·뉴스·목표주가를 묻지
     않고, `app/reports.py` 가 계산한 숫자만 JSON 으로 넘긴 뒤 **그 안에서만**
     서술하게 합니다. 넘기지 않은 것은 "데이터에 없다"고 말하도록 지시합니다.

  2. 점 예측을 금지합니다. 이 앱 전체가 "내일 얼마"를 하지 않는다는 원칙 위에
     서 있는데, AI 서술만 예외로 두면 원칙이 무의미해집니다.

  3. 출력 형식을 고정합니다. 마지막 절은 항상 '반대 해석'과 '이 분석의 한계'
     입니다. 강세 서술만 남으면 사용자는 그것을 추천으로 읽습니다.

  4. **근거 등급을 함께 넘깁니다.** 지지·저항이라고 다 같은 지지·저항이
     아닙니다. 거래량 밀집대는 처분효과 문헌(Grinblatt & Han 2005)에 뿌리가
     있고 이동평균선은 데이터 스누핑 보정 후 대부분 사라진 근거(Sullivan 외
     1999)입니다. 등급을 주지 않으면 모델은 둘을 같은 무게로 씁니다 -- 그러면
     한계 문구가 형식적인 면피가 되고, 사용자는 어떤 선을 더 믿을지 알 수
     없습니다.

분할 매수·매도에 대한 입장:
    "사라/팔라"는 여전히 금지입니다. 다만 사용자가 이미 매매하기로 정한
    경우의 **체결 구조**(어느 구간에 몇 %, 어디서 틀렸다고 인정할지)는
    계산의 영역이고, `app/plan.py` 가 이미 그 숫자를 만들어 둡니다. 모델은
    그 숫자를 설명할 뿐 새로 만들지 않습니다.

모델의 지식 시점(cutoff)은 알 수 없고 데이터는 오늘 것이므로, 기억에 의존한
서술은 그 자체로 틀릴 위험이 큽니다 -- 그래서 근거를 전부 프롬프트에 싣습니다.
"""

from __future__ import annotations

import json
from typing import Any

SYSTEM = """당신은 한국어로 답하는 주식 데이터 분석 보조자입니다.

절대 규칙:
1. 제공된 JSON 데이터에 있는 숫자만 사용하십시오. 실적, 뉴스, 공시, 목표주가,
   경쟁사 정보 등 데이터에 없는 것은 절대 지어내지 마십시오. 필요하면
   "이 앱의 데이터에는 없습니다"라고 쓰십시오.
2. 점 예측을 하지 마십시오. "내일 X원", "3개월 뒤 Y%" 같은 문장은 금지입니다.
   방향과 조건("~이 유지되면", "~을 밑돌면")으로 서술하십시오.
3. 매수/매도 판단을 대신하지 마십시오. "지금 사십시오", "비중을 늘리십시오",
   "이 종목은 유망합니다" 같은 문장은 금지입니다.
   다만 데이터에 `plan` 이 있으면, **사용자가 이미 매매하기로 정했다는 가정
   아래의 체결 구조**(어느 구간에 몇 %, 어디서 전제가 깨지는지)는 서술해도
   됩니다. 그때도 "매매 여부 자체는 당신의 판단"이라는 전제를 분명히 쓰고,
   `plan` 에 있는 비중·가격·확률을 그대로 인용하십시오. 직접 계산하지 마십시오.
4. 가격 수준은 데이터의 `zones` 와 `plan` 에 있는 값만 쓰십시오. 새로운
   지지선·저항선·목표가를 만들어내지 마십시오.
5. 숫자를 인용할 때는 데이터의 값을 그대로 쓰십시오. 반올림은 허용되나
   없는 값을 추정하지 마십시오. null 은 "측정 불가"입니다.
6. 근거의 세기를 구분하십시오. `zones.method_notes` 의 evidence_weight 가
   낮은 방법(특히 이동평균선)에만 기댄 수준은 그렇다고 밝히십시오. 확률은
   무추세·정규분포 가정에서 나온 터치 확률이며 예측이 아닙니다.

출력은 마크다운이며, 마지막 두 절은 반드시 다음이어야 합니다:
## 반대 해석
## 이 분석의 한계
"""

_COMMON_TAIL = """
분량은 400~700자 정도로 압축하십시오. 표는 쓰지 말고 짧은 문단과 불릿으로
쓰십시오. 데이터의 caveats 배열에 담긴 한계는 '이 분석의 한계' 절에 반영하십시오.
"""

# 종목 분석은 다루는 항목이 많아 같은 분량으로는 각 절이 한 문장이 됩니다.
_STOCK_TAIL = """
분량은 1,200~2,000자로 쓰십시오. 표는 쓰지 말고 짧은 문단과 불릿으로 쓰십시오.
가격은 데이터의 표기를 따르되 읽기 쉽게 반올림해도 됩니다.
데이터의 caveats 배열에 담긴 한계는 '이 분석의 한계' 절에 반영하십시오.
"""


def stock_prompt(report: dict[str, Any], forecast: dict | None = None) -> str:
    label = report.get("name") or report.get("ticker")
    body: dict[str, Any] = {"stock": report}
    if forecast:
        body["forecast"] = forecast
    has_forecast = "있습니다" if forecast else "없습니다"

    return f"""아래는 {report.get("market")} 시장 종목 **{label}
({report.get("ticker")})** 의 계산된 지표입니다. 워크포워드 예측 블록은 {has_forecast}.

다음 절 구성으로 서술하십시오:

## 지금 국면
`regime` 을 먼저 읽으십시오. 추세인지 횡보인지, 변동성이 최근 2년 중 어느
위치인지(percentile_2y), 52주 레인지의 몇 % 지점인지를 숫자와 함께 씁니다.
그 다음에야 개별 지표(RSI, 이동평균, 상대강도)를 국면 안에서 해석하십시오.
같은 RSI 40 이라도 횡보장과 하락 추세에서 뜻이 다르다는 점을 반영하십시오.

## 지지 구간 셋
`zones.supports` 의 항목을 가까운 것부터 순서대로 쓰십시오. 각각에 대해
반드시 넷을 밝히십시오: (1) 구간 범위(low~high, 점이 아니라 구간임을 명시),
(2) 어떤 방법들이 이 가격을 지목했는지(methods)와 그 근거 문구(evidence),
(3) 신뢰도(confidence)와 겹친 방법 수(n_methods), (4) 21거래일 내 도달 확률
(touch_prob_21d). 근거가 이동평균 하나뿐인 구간은 "가장 약한 근거"라고
명시하십시오. `zones` 에 없는 가격을 추가하지 마십시오.

## 저항 구간 셋
같은 형식으로 `zones.resistances` 를 쓰십시오.

## 분할 매수 골격
`plan.entry` 를 설명하십시오. 이 종목을 매수할지 여부는 사용자의 판단이라는
전제를 먼저 한 문장으로 밝히고, 그 다음에 각 단계의 가격대와 비중(weight)을
데이터 그대로 인용하십시오. 반드시 포함할 것:
  - '즉시' 단계가 있는 이유 (`prob_no_limit_fill_21d`: 지정가만 걸면 그 확률로
    한 주도 못 삽니다)
  - `partial_fills` 에서 1차만 체결된 경우와 전량 체결된 경우의 평균단가 차이
  - `invalidation.price` 와 그 의미 (여기 아래에서는 '되돌림 매수' 전제가 깨짐)
  - `risk` 의 1R 비율과 `max_position_for_1pct_account_risk`
  - 분할 매수가 기대수익을 높이는 기법이 **아니라는** 점 (caveats 에 근거 있음)

## 분할 매도 골격
`plan.exit` 을 같은 방식으로. `weight_still_held`(모든 저항에 도달해도 남는
비중)를 반드시 언급하고, 분할 매도의 근거가 매수와 다르다는 점(처분효과에
대한 사전 약속)을 `why_split` 을 근거로 쓰십시오.

## 앞으로의 조건부 시나리오
`scenarios` 배열만 사용하십시오. 각 시나리오의 trigger(어떤 가격을 어떻게
넘거나 밑돌면), touch_prob, then(그때 무엇이 바뀌는지)을 쓰십시오. 확률이
서로 배타적이지 않다는 점(prob_note)을 한 번 밝히십시오. forecast 블록이
있으면 그 방향 확률과 품질 지표(skill_score 또는 oos_r2_cross)를 함께 읽되,
품질이 낮으면 낮다고 쓰십시오. 점 예측은 여전히 금지입니다.

## 반대 해석
이 분석이 틀리는 가장 그럴듯한 경로를 쓰십시오. 지지 구간이 지지가 아니라
하락 추세의 경유지일 가능성, 확률 계산의 가정(무추세·정규분포·일정 변동성)이
깨지는 경우를 포함하십시오.

## 이 분석의 한계
{_STOCK_TAIL}
데이터:
```json
{_dump(body)}
```"""


def sector_prompt(report: dict[str, Any]) -> str:
    return f"""아래는 {report.get("market")} 시장 **{report.get("sector")}** 업종의
계산된 집계입니다.

다음 절 구성으로 서술하십시오:
## 업종 현황
수익률·상대강도·breadth 를 함께 읽어 서술. breadth 가 0.5 미만이면 소수 종목이
끌어올린 것이므로 반드시 그렇게 쓰십시오.
## 업종 내부 분화
leaders 와 laggards 의 격차가 무엇을 뜻하는지.
## 지켜볼 지점
## 반대 해석
## 이 분석의 한계
{_COMMON_TAIL}
데이터:
```json
{_dump(report)}
```"""


def market_prompt(report: dict[str, Any]) -> str:
    market_kr = "한국" if report.get("market") == "KR" else "미국"
    return f"""아래는 **{market_kr} 시장** 전체(수집된 유니버스 동일가중)의 계산된
집계입니다.

다음 절 구성으로 서술하십시오:
## 시장 상태
index_proxy 와 internals(60일선 위 비율, 52주 고가 근접 비율, 중간 변동성)를
함께 읽어 서술. 평균 수익률과 내부지표가 어긋나면 그 점을 지적하십시오.
## 업종 지형
어떤 업종이 앞서고 뒤처지는지, 그리고 그 분산이 넓은지 좁은지.
## 주목할 종목
attention 배열에 있는 종목 중 3~5개를 골라, **각각 어떤 규칙에 걸렸는지**를
근거로 왜 관찰 대상인지 쓰십시오. 매수 추천이 아니라 관찰 후보임을 명시하고,
attention 에 없는 종목을 새로 추가하지 마십시오.
## 반대 해석
## 이 분석의 한계
{_COMMON_TAIL}
여기서 '시장'은 공식 지수가 아니라 수집된 시총 상위 종목의 동일가중 평균임을
분석 안에서 한 번 명시하십시오.

데이터:
```json
{_dump(report)}
```"""


def _dump(payload: Any) -> str:
    """프롬프트에 실을 JSON. 너무 크면 모델이 앞부분만 보고 답합니다."""
    return json.dumps(payload, ensure_ascii=False, indent=1, default=str)
